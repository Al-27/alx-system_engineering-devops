#!/usr/bin/env bash
#echo "give me a bottle of rum!"

list=$(ls) 
#list=$( grep -Eo "[^-].*" <<< "$(grep -Eo "[^0-9a-zA-Z].*" <<< "$list")" )

for li in $list
do
    tmp=$( grep -Eo "[^-].*" <<< "$(grep -Eo "[^0-9a-zA-Z].*" <<< "$li")" )
    echo "$tmp"
done